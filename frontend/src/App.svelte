<!--
  App.svelte is the root of the PitMate UI. Later it will host the tab bar
  (Live Data, Strategy Calls, Driver Coaching, Driver Vs., Car Management,
  Settings) and the global overlays (flag/safety-car banners, event timeline).

  Session 1 scaffolding: placeholder only. No WebSocket connection or tabs yet.
-->
<script>
  // TODO: connect to the Go server's WebSocket, parse telemetry.Frame JSON,
  // and feed it into the tab components.
</script>

<main>
  <h1>PitMate</h1>
  <p>Early scaffolding — the strategist cockpit UI is not built yet.</p>
</main>
